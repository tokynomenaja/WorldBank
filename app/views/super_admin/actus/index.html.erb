
	<div class="card" style="width: 18rem;">
				<% @actus.each do |p|%>
		 	 <div class="card-body">
			    <h5 class="card-title"><%= p.title %></h5>
			    <a href="<%= super_admin_actu_path(p.id)%>" class="btn btn-primary">VOIR</a>
		  </div>
		  <%end%>
</div>