
<div class=" container d-flex flex-wrap sans-head">
	
			<% @pubs.each do |p|%>
				<div class="col-sm-6 mb-3">
					<div class="card">
						<div class="card-body">
						<h5 class="card-title"><%= p.title %></h5>
						<p class="card-text"><%= p.description %></p>
						<p><a href="<%= super_admin_publication_path(p.id)%>"> Voir </a></p>
						</div>
					</div>
				</div>
			<%end%>
	</div>