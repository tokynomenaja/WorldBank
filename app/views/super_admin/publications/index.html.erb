<div class="sans-head">
	<div class="row justify-content-center">
		<% @pubs.each do |p|%>
			<div class="col-md-3 card mx-1 mb-2 position-relative">
				<div class="card-body">
					 	 	<div class="card-title">
						    <h5 class="primary-color"><%= p.title %></h5>
						    </div>
						<div class="card-text">
							<p class="card-text text-muted mb-5 truncate-pub"><%= p.description %></p>
						</div>
						<p class="mt-3 position-absolute more-info-pub"><a href="<%= super_admin_publication_path(p.id)%>" class="btn-forgot underline">Voir</a></p>
				</div>
			</div>
      <%end%>
	</div>
</div>